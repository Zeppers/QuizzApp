<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.aeroz.quizzapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SplashscreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".OnboardingActivity" />
        <activity android:name=".RoleplayActivity" />
        <activity android:name=".SigninActivity" />
        <activity android:name=".PHomeActivity" />
        <activity android:name=".PPublicActivity" />
        <activity android:name=".PPrivateActivity" />
        <activity android:name=".SHomeActivity" />
        <activity android:name=".SQuestionActivity" />
        <activity android:name=".PQuizDetailActivity" />
        <activity android:name=".SResultActivity"></activity>
        <activity android:name=".SQuizPreviewActivity"></activity>
    </application>

</manifest>